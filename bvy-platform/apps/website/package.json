{
  "name": "bvy-website",
  "version": "1.0.0",
  "private": true,
  "description": "Site public de BVY Accounting & Tax Services — serveur Node.js sans dépendances.",
  "main": "server.js",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "test": "node --test test/*.test.js",
    "dev": "node build.js && node server.js"
  }
}
